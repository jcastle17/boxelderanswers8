
const header=document.querySelector('.site-header');const toggle=document.querySelector('.mobile-toggle');const links=document.querySelector('.nav-links');function updateHeader(){if(!header)return;if(window.scrollY>18){header.classList.add('is-scrolled')}else if(!header.classList.contains('solid')){header.classList.remove('is-scrolled')}}window.addEventListener('scroll',updateHeader);updateHeader();if(toggle&&links){toggle.addEventListener('click',()=>{const open=links.classList.toggle('open');toggle.setAttribute('aria-expanded',open?'true':'false')})}document.querySelectorAll('[data-copy]').forEach(btn=>{btn.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(btn.dataset.copy);const old=btn.textContent;btn.textContent='Copied';setTimeout(()=>btn.textContent=old,1200)}catch(e){}})});
